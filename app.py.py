import streamlit as st
import pandas as pd
import io

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="Excel to CSV Converter", page_icon="📂")

st.title("📂 แปลงไฟล์ Excel เป็น CSV")
st.write("ระบบจะคัดกรองเฉพาะรถที่มี 'ทะเบียน' และเลือกคอลัมน์ให้ประหยัดเวลา!")

# ==========================================
# รายชื่อคอลัมน์ที่ต้องการ (Config)
# ==========================================
target_columns = [
    'หมายเลขตัวถัง', 
    'จังหวัด', 
    'ทะเบียน',
    'วันที่จดทะเบียน',
    'ผู้ถือกรรมสิทธิ์',
    'ผู้ถือครอง',
    'แผ่นป้าย',
    'ป้ายวงกลม',
    'คู่มือ',
    'สาขา'
]
# ==========================================

# 1. ส่วนอัปโหลดไฟล์
uploaded_file = st.file_uploader("เลือกไฟล์ Excel (.xlsx)", type=['xlsx'])

if uploaded_file is not None:
    try:
        # อ่านไฟล์ Excel
        df = pd.read_excel(uploaded_file)
        
        # เก็บจำนวนแถวเดิมไว้เทียบ
        original_rows = len(df)

        # ==========================================
        # 🟢 ส่วนกรองข้อมูล (Filter Logic)
        # ==========================================
        if 'ทะเบียน' in df.columns:
            # ลบแถวที่ 'ทะเบียน' เป็นค่าว่าง (NaN/Null)
            df = df.dropna(subset=['ทะเบียน'])
            
            # คำนวณจำนวนที่ถูกตัดออก
            filtered_rows = len(df)
            removed_rows = original_rows - filtered_rows
            
            st.success(f"✅ อัปโหลดสำเร็จ! (ทั้งหมด {original_rows} แถว)")
            
            if removed_rows > 0:
                st.info(f"🧹 ระบบได้กรองรถที่ **ไม่มีทะเบียน** ออกไปจำนวน **{removed_rows}** แถว (เหลือข้อมูลพร้อมใช้ **{filtered_rows}** แถว)")
            else:
                st.info("👍 ข้อมูลครบถ้วน ทุกคันมีเลขทะเบียน")
        else:
            st.warning("⚠️ ไม่พบคอลัมน์ 'ทะเบียน' ในไฟล์นี้ ระบบจึงไม่ได้ทำการกรองข้อมูล")
        # ==========================================
        
        # 2. ส่วนเลือกคอลัมน์ (Column Selector)
        all_columns = df.columns.tolist()

        # คำนวณหาว่าคอลัมน์ไหนที่มีอยู่จริงในไฟล์บ้าง (กัน Error)
        default_selection = [col for col in target_columns if col in all_columns]
        missing_columns = [col for col in target_columns if col not in all_columns]

        # แจ้งเตือนถ้าไฟล์ Excel ไม่มีคอลัมน์ที่ต้องการบางตัว
        if missing_columns:
            st.warning(f"⚠️ ไม่พบคอลัมน์ต่อไปนี้ในไฟล์ Excel ของคุณ: {missing_columns}")

        st.subheader("เลือกคอลัมน์ที่ต้องการ:")
        selected_columns = st.multiselect(
            "ตรวจสอบรายชื่อคอลัมน์ (ระบบเลือกให้แล้ว)",
            options=all_columns,
            default=default_selection
        )

        if selected_columns:
            # กรองข้อมูลตามที่เลือก
            df_selected = df[selected_columns]
            
            # แสดงตัวอย่างข้อมูล
            st.write("### ตัวอย่างข้อมูลผลลัพธ์ (เฉพาะที่มีทะเบียน):")
            st.dataframe(df_selected.head(10))
            
            # 3. ส่วนแปลงเป็น CSV และดาวน์โหลด
            csv_buffer = df_selected.to_csv(index=False).encode('utf-8-sig')
            
            st.download_button(
                label="⬇️ ดาวน์โหลดไฟล์ CSV",
                data=csv_buffer,
                file_name="converted_data.csv",
                mime="text/csv",
            )
        else:
            st.warning("กรุณาเลือกอย่างน้อย 1 คอลัมน์ครับ")
            
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาด: {e}")
