import streamlit as st
import pandas as pd
import io

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="Excel to CSV Converter", page_icon="📂")

st.title("📂 แปลงไฟล์ Excel เป็น CSV")
st.write("ระบบจะคำนวณ 'วันหมดอายุ' (จดทะเบียน + 1 ปี) ให้โดยอัตโนมัติ")

# ==========================================
# รายชื่อคอลัมน์ที่ต้องการ (Config)
# ==========================================
target_columns = [
    'หมายเลขตัวถัง', 
    'จังหวัด', 
    'ทะเบียน',
    'วันที่จดทะเบียน',
    'วันหมดอายุ',       # <--- เพิ่มคอลัมน์นี้เข้าไปในรายการที่ต้องการ
    'ผู้ถือกรรมสิทธิ์',
    'ผู้ถือครอง',
    'แผ่นป้าย',
    'ป้ายวงกลม',
    'คู่มือ',
    'สาขา'
]
# ==========================================

# 1. ส่วนอัปโหลดไฟล์
uploaded_file = st.file_uploader("เลือกไฟล์ Excel (.xlsx)", type=['xlsx'])

if uploaded_file is not None:
    try:
        # อ่านไฟล์ Excel
        df = pd.read_excel(uploaded_file)
        
        # ==========================================
        # 🕒 ส่วนคำนวณวันหมดอายุ (เพิ่มใหม่ตรงนี้)
        # ==========================================
        if 'วันที่จดทะเบียน' in df.columns:
            # 1. แปลงให้เป็น format วันที่ที่ถูกต้อง (เผื่อ Excel มาเป็น Text)
            df['วันที่จดทะเบียน'] = pd.to_datetime(df['วันที่จดทะเบียน'], errors='coerce')
            
            # 2. คำนวณวันหมดอายุ (บวกเพิ่ม 1 ปี)
            # ใช้ DateOffset จะแม่นยำเรื่องปีอธิกสุรทิน (Leap year)
            df['วันหมดอายุ'] = df['วันที่จดทะเบียน'] + pd.DateOffset(years=1)
            
            # 3. (Optional) จัด Format ให้สวยงาม ตัดเวลา 00:00:00 ทิ้ง
            # ถ้าอยากได้แบบมีเวลาด้วย ให้ลบบรรทัดข้างล่างนี้ออก
            df['วันหมดอายุ'] = df['วันหมดอายุ'].dt.date
            
            st.success("✅ คำนวณวันหมดอายุ (+1 ปี) เรียบร้อยแล้ว")
        else:
            st.warning("⚠️ ไม่พบคอลัมน์ 'วันที่จดทะเบียน' จึงไม่สามารถคำนวณวันหมดอายุได้")
        # ==========================================

        st.success(f"โหลดข้อมูลสำเร็จ! (ทั้งหมด {len(df)} แถว)")
        
        # 2. ส่วนเลือกคอลัมน์ (Column Selector)
        all_columns = df.columns.tolist()

        # คำนวณหาว่าคอลัมน์ไหนที่มีอยู่จริงในไฟล์บ้าง (รวมถึงคอลัมน์ใหม่ที่เพิ่งสร้างด้วย)
        default_selection = [col for col in target_columns if col in all_columns]
        missing_columns = [col for col in target_columns if col not in all_columns]

        # แจ้งเตือนถ้าหาคอลัมน์ไม่เจอ
        if missing_columns:
            # กรองเอาเฉพาะที่ไม่ได้เกิดจากการสร้างเอง (เพราะวันหมดอายุเราสร้างเองอาจจะยังไม่อยู่ในไฟล์ต้นฉบับ)
            real_missing = [c for c in missing_columns if c != 'วันหมดอายุ']
            if real_missing:
                st.warning(f"⚠️ ไม่พบคอลัมน์ต่อไปนี้ในไฟล์ต้นฉบับ: {real_missing}")

        st.subheader("เลือกคอลัมน์ที่ต้องการ:")
        selected_columns = st.multiselect(
            "ตรวจสอบรายชื่อคอลัมน์ (ระบบเลือกให้แล้ว)",
            options=all_columns,
            default=default_selection
        )

        if selected_columns:
            # กรองข้อมูลตามที่เลือก
            df_selected = df[selected_columns]
            
            # แสดงตัวอย่างข้อมูล
            st.write("### ตัวอย่างข้อมูลผลลัพธ์:")
            st.dataframe(df_selected.head(10))
            
            # 3. ส่วนแปลงเป็น CSV และดาวน์โหลด
            csv_buffer = df_selected.to_csv(index=False).encode('utf-8-sig')
            
            st.download_button(
                label="⬇️ ดาวน์โหลดไฟล์ CSV",
                data=csv_buffer,
                file_name="converted_data.csv",
                mime="text/csv",
            )
        else:
            st.warning("กรุณาเลือกอย่างน้อย 1 คอลัมน์ครับ")
            
    except Exception as e:
        st.error(f"เกิดข้อผิดพลาด: {e}")
